# it-infra

Bootstrapped by the cowork kernel's `ingest-project.py` on 2026-05-20T11:15:24Z (KM316/KM328).

Project prefix: `IM`. The canonical WBS lives in the kernel at `_cowork/dashboard-it-infra.json` -- this repo carries code only.

See `AGENTS.md` at this repo root for agent-orientation. See the kernel `_cowork/working-agreement.md` for the methodology.
